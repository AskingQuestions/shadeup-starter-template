{
  "name": "shadeup-starer-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx concurrently \"cd src && npx shadeup watch main.shadeup\" \"vite\"",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@shadeup/cli": "^1.3.0",
    "concurrently": "^8.2.2",
    "typescript": "^5.2.2",
    "vite": "^5.2.8"
  },
  "dependencies": {
    "shadeup": "^1.3.6"
  }
}
